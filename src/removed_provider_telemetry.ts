export const recordRemovedProviderTelemetry = (_input: Record<string, unknown>): Promise<void> => Promise.resolve();
export const getRemovedProviderTelemetryView = (): Promise<Record<string, never>> => Promise.resolve({});
