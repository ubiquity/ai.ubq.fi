{
  "id": "fail-003",
  "category": "failure",
  "title": "Recover from invalid tool arguments",
  "description": "A read call is issued with a non-string path and rejected; retry with valid arguments and record the note body in answer.txt.",
  "fixture": "fail-003",
  "fixture_revision": "sha256:f42aad12cc2962b7999f43bfef40dfb505cbf8de44d9fc30db43aa885277bd3e",
  "timeout_ms": 60000,
  "max_tool_calls": 40,
  "min_tool_calls": 3,
  "min_model_calls": 0,
  "allowed_write_scope": ["**"],
  "verify": { "command": "sh tests/run.sh", "timeout_ms": 20000 },
  "oracle": {
    "file_checks": [{ "path": "answer.txt", "kind": "equals", "value": "note body" }]
  },
  "scripted_trail": [
    {
      "tool": "filesystem.read",
      "args": { "path": 42 },
      "expect": { "ok": false, "error_contains": "invalid arguments" }
    },
    {
      "tool": "filesystem.read",
      "args": { "path": "data/note.txt" },
      "recovery": true,
      "expect": { "ok": true, "output_contains": ["note body"] }
    },
    { "tool": "editor.apply_patch", "args": { "path": "answer.txt", "add": true, "new": "note body" } }
  ]
}
